import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  singleStar: {
    width: 10,
    height: 10,
  },
  starSize: {},
  list: {},
  ratingContainer: {
    marginBottom: 0,
    paddingBottom: 0,
    marginLeft: 7,
    alignContent: 'center',
  },
});

export default styles;
