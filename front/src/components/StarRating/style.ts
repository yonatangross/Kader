import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  singleStar: {
    width: 15,
    height: 15,
  },
  starSize: {},
  list: {},
  ratingContainer: {
    margin: 5,
    marginBottom: 0,
    paddingBottom: 0,
    flex: 1,
  },
});

export default styles;
