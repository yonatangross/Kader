import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  singleStar: {
    color: '#f3a953',
    alignSelf: 'center',
    marginTop:5
  },
  starSize: {},
  list: {},
  ratingContainer: {
    alignContent: 'center',
  },
});

export default styles;
