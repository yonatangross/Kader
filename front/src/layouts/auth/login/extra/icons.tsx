import React from 'react';
import { ImageStyle } from 'react-native';
import { Icon, IconElement } from '@ui-kitten/components';

export const ArrowForwardIcon = (style: any) => <Icon {...style} name="arrow-forward-outline" />;

export const GoogleIcon = (style: any) => <Icon {...style} name="google" />;

export const FacebookIcon = (style: any) => <Icon {...style} name="facebook" />;

export const TwitterIcon = (style: any) => <Icon {...style} name="twitter" />;
